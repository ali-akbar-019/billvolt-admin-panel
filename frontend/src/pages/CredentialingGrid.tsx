import { PagePlaceholder } from '../components/PagePlaceholder';

export function CredentialingGrid() {
  return <PagePlaceholder title="Credentialing grid" description="Provider by payer credentialing status." />;
}
